{
    "title": "modulo",
    "body": [
        {
            "paragraph": "The modulo operator <span class=\"code\">%</span> returns the remainder of a division"
        },
        {
            "example": "print((29 % 5)) # prints 4\nprint((28 % 5)) # prints 3\nprint((27 % 5)) # prints 2\nprint((26 % 5)) # prints 1\nprint((25 % 5)) # prints 0"
        },
        {
            "paragraph": "the mod operator only works on numbers."
        }
    ]
}
